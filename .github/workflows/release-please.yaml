name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v4
        with:
          release-type: python
          # https://github.com/google-github-actions/release-please-action/issues/396#issuecomment-1136479955
          package-name: random-travelers
          version-path: random_travelers/__init__.py
